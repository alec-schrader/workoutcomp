import * as React from 'react';
import { Button, Container, dividerClasses, Grid, Item } from "@mui/material";


export default function NewComp() {
    return (
        <Container sx={{
            textAlign: 'center',
            alignItems:'center'
          }}>
            <h1>A new competition eh?</h1>
        </Container>
    );
};  